<?xml version="1.0" encoding="UTF-8"?>
  <urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    <url>
      <% if (pages.length > 0) {
        pages.forEach((p) => { %> 
        <loc><%= origin %>/<%= p %></loc>
      <% })
      } %>
    </url>
  </urlset>
